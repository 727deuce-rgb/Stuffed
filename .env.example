# Environment variables

Copy this file to `.env.local` for local development. Do not commit `.env.local`.

```env
# Add provider keys only when the corresponding integration is implemented.
# AI_PROVIDER_API_KEY=
# BASE_RPC_URL=
# SOLANA_RPC_URL=
# KYC_PROVIDER_KEY=
```
