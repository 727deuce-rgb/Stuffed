"use client";

import { useState } from "react";
import { Activity, Braces, ChevronDown, Cloud, Code2, Database, GitBranch, GitCommitHorizontal, Globe2, Lock, Play, Plus, Rocket, Search, Settings2, Sparkles, Terminal, Users, Zap } from "lucide-react";
import { SaaSOverview } from "@/components/saas-overview";

const files = ["app/page.tsx", "app/layout.tsx", "lib/supabase/client.ts", "supabase/migrations/001_workspace.sql", "README.md"];
const logs = ["▲ Next.js 14.2.5", "✓ Supabase client initialized", "✓ Preview environment ready", "○ Local: http://localhost:3000"];

export default function Home() {
  const [activeFile, setActiveFile] = useState(files[0]);
  const [running, setRunning] = useState(false);
  const [tab, setTab] = useState<"preview" | "terminal">("preview");
  const [published, setPublished] = useState(false);
  return <main className="forge-app"><header className="topbar"><div className="forge-brand"><span className="forge-mark">F</span><span>Forge<span className="brand-fade">cloud</span></span></div><div className="workspace-switch"><span className="status-dot" /> omni-studio <ChevronDown size={14} /></div><div className="top-actions"><button className="icon-button"><Search size={16} /></button><button className="icon-button"><Users size={16} /></button><button className="top-avatar">JD</button></div></header><div className="app-shell"><aside className="rail"><button className="rail-active"><Code2 size={18} /><span>Build</span></button><button><GitBranch size={18} /><span>Git</span></button><button><Database size={18} /><span>Data</span></button><button><Cloud size={18} /><span>Storage</span></button><button><Activity size={18} /><span>Usage</span></button><div className="rail-spacer" /><button><Settings2 size={18} /><span>Settings</span></button></aside><section className="workspace"><div className="workspace-head"><div><div className="crumb"><span>omni-studio</span><span>/</span><strong>web-app</strong><span className="private-pill"><Lock size={11} /> Private</span></div><h1>Build, ship, and scale in one place.</h1><p>Git-native collaboration, an instant Next.js runtime, and Supabase data that is ready when you are.</p></div><div className="head-buttons"><button className="outline-button"><GitCommitHorizontal size={15} /> Commit <span className="kbd">⌘↵</span></button><button className="primary-button" onClick={() => { setRunning(true); setTab("preview"); }}><Play size={15} /> {running ? "Running" : "Run"}</button></div></div><SaaSOverview /><div className="project-tabs"><button className="tab-active">Workspace</button><button>Pull requests <span>2</span></button><button>Deployments <span>3</span></button><button>Members <span>4</span></button><div className="tab-fill" /><span className="branch"><GitBranch size={14} /> main <ChevronDown size={13} /></span></div><div className="builder"><aside className="file-panel"><div className="panel-label"><span>EXPLORER</span><button><Plus size={14} /></button></div><div className="repo-root"><ChevronDown size={13} /> <strong>WEB-APP</strong><span className="muted-count">{files.length}</span></div>{files.map((file) => <button className={`file-row ${activeFile === file ? "selected" : ""}`} key={file} onClick={() => setActiveFile(file)}><span className={`file-icon ${file.endsWith("tsx") ? "tsx" : file.endsWith("ts") ? "ts" : file.endsWith("sql") ? "sql" : "md"}`}>{file.endsWith("tsx") ? "TSX" : file.endsWith("ts") ? "TS" : file.endsWith("sql") ? "SQL" : "M"}</span>{file}</button>)}<div className="file-bottom"><div><GitBranch size={13} /> main</div><div><GitCommitHorizontal size={13} /> synced</div></div></aside><section className="editor-panel"><div className="editor-tabs"><div className="editor-tab"><span className="tsx-dot">TS</span>{activeFile}<span className="close">×</span></div><div className="editor-spacer" /><span className="editor-meta">Prettier</span><button className="icon-button"><Sparkles size={15} /></button></div><div className="code-editor"><div className="line-numbers">{Array.from({ length: 22 }, (_, i) => <span key={i}>{i + 1}</span>)}</div><pre><code><span className="kw">import</span> <span className="plain">&#123; createClient &#125;</span> <span className="kw">from</span> <span className="str">&quot;@/lib/supabase/client&quot;</span>{"\n\n"}<span className="kw">export default function</span> <span className="fn">Workspace</span><span className="plain">() &#123;</span>{"\n"}  <span className="kw">return</span> <span className="plain">(</span>{"\n"}    <span className="tag">&lt;main</span> <span className="attr">className</span><span className="plain">=</span><span className="str">&quot;workspace&quot;</span><span className="tag">&gt;</span>{"\n"}      <span className="tag">&lt;h1&gt;</span><span className="plain">Your app, connected.</span><span className="tag">&lt;/h1&gt;</span>{"\n"}      <span className="tag">&lt;button</span> <span className="attr">onClick</span><span className="plain">=</span><span className="str">&#123;deploy&#125;</span><span className="tag">&gt;</span>{"\n"}        <span className="plain">Deploy project</span>{"\n"}      <span className="tag">&lt;/button&gt;</span>{"\n"}    <span className="tag">&lt;/main&gt;</span>{"\n"}  <span className="plain">);</span>{"\n"}<span className="plain">&#125;</span></code></pre></div><div className="editor-status"><span>Ln 1, Col 1</span><span>Spaces: 2</span><span>TypeScript React</span><span className="status-spacer" /><span>main*</span></div></section><section className="right-panel"><div className="right-tabs"><button className={tab === "preview" ? "right-active" : ""} onClick={() => setTab("preview")}><Globe2 size={14} /> Preview</button><button className={tab === "terminal" ? "right-active" : ""} onClick={() => setTab("terminal")}><Terminal size={14} /> Terminal</button></div>{tab === "preview" ? <div className="preview"><div className="preview-toolbar"><span className="live-dot" /> {running ? "localhost:3000" : "Preview paused"}<button onClick={() => setRunning(true)}><Rocket size={13} /> Open</button></div><div className="preview-page"><div className="preview-nav"><span className="preview-logo">F</span><span>forgecloud</span><span className="preview-nav-fill" /><span>Docs</span><span>Git</span><span className="preview-user">JD</span></div><div className="preview-content"><Sparkles size={24} color="#9ce56a" /><h2>Build something<br /><em>useful.</em></h2><p>Your app, connected in seconds.</p><button onClick={() => setPublished(true)}><Rocket size={14} /> {published ? "Published" : "Deploy project"}</button>{published && <small className="published-note">✓ Deployed to preview environment</small>}</div></div></div> : <div className="terminal"><div className="terminal-head"><span className="live-dot" /> bash <span>×</span></div>{logs.map((log) => <p key={log} className={log.startsWith("✓") ? "success" : ""}>{log}</p>)}<p className="cursor">$ <span>_</span></p></div>}</section></div><div className="bottom-cards"><article><div className="card-icon green"><Zap size={17} /></div><div><strong>Instant environments</strong><p>Every branch gets its own live preview.</p></div><span className="card-arrow">→</span></article><article><div className="card-icon purple"><Database size={17} /></div><div><strong>Database included</strong><p>Tables, auth, storage, and APIs in one click.</p></div><span className="card-arrow">→</span></article><article><div className="card-icon blue"><GitBranch size={17} /></div><div><strong>Git-native by default</strong><p>Commits, reviews, and deploys stay together.</p></div><span className="card-arrow">→</span></article></div></section></div></main>;
}
